# Why Build a Mini Agent?

## 1. 什么是 Agent？
不是 ChatBot。
Agent 的目标是： 为了完成目标，不断执行 Action。

## 2. 为什么不用 LangChain？

因为：我们希望理解：
每一个 Workflow 是怎么工作的。
不是直接调用封装好的 API。

## 3. 为什么一步一步实现？

因为：只有逐步增加能力，才能知道： 到底：
Tool、 Planning、 Reflection、 Memory
分别带来了什么提升。

## 4. 我们的开发原则
一次只增加一个能力。
一次只验证一个假设。
一次只解决一个问题。